"""
APR 2022

@author: KevinAagaard
"""

import pandas as pd


"""
Read in and manipulate data
"""
#get_ipython().run_line_magic('matplotlib', 'inline') # Inline plotting
#get_ipython().run_line_magic('matplotlib', 'qt')     # New window plotting

data = pd.read_csv("Engagement Survey Pilot _Cleaned for CoP.csv")

# View Data
data.head()
data.describe()
data.shape

# # Rename columns
# data = data.rename(columns={"":""})

# # Transform the data frame from wide to long
# data_melt = pd.melt(data,
#                     id_vars=[],
#                     var_name = '',
#                     value_name = '')
